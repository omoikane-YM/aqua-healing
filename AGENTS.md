# aqua-healing AI 運用ルール

この文書は、このリポジトリを AI が修正・運営するときの最上位の行動ルールです。

## 指示の優先順位

1. ユーザーからの最新の明示的な指示
2. 構造化された正式データ
3. `SITE_FACTS.md`
4. `AFFILIATE_RULES.md`
5. `AGENTS.md`
6. 現在の正常なサイト実装
7. AI による改善案・一般的なベストプラクティス

最新の明示的な指示がある場合も、指定された範囲だけを変更する。指示されていない商品、Amazon リンク、価格、文章、画像、レイアウト、デザイン、SEO、URL、依存関係を連鎖的に変更しない。

## 基本方針

- スマートフォン表示を最優先する。
- 現在の aqua-healing のデザイン、世界観、配色、フォント、文章、画像を維持する。
- レイアウトを勝手に全面刷新しない。
- 既存ユーザーの操作導線を壊さない。
- ページ URL、slug、内部リンクを不要に変更しない。
- title、meta description、canonical URL、OGP、sitemap、`robots.txt` を明示的な指示なしに変更しない。
- `sitemap.xml` は公開済み URL の一覧を基準とし、ページ追加・削除時だけ同期する。URL を推測で追加せず、削除時は既存インデックスへの影響とリダイレクト要否を確認する。
- `robots.txt` の Sitemap URL は正式な公開 URL と一致させる。
- 必要最小限の変更を原則とし、依頼と関係ないリファクタリングを行わない。
- 正常稼働中のフレームワークを AI 判断で変更しない。大規模な技術変更は実装せず提案として報告する。
- 不明な情報は推測せず、「未確認」または「現在のコードから確認できない」と記録する。
- 既存情報同士に矛盾がある場合は統一せず、「差異あり」と報告してユーザーの判断を待つ。

## 正式データ

- 機械可読な正式データは `site-facts.json`、人間向けの一覧は `SITE_FACTS.md` とする。
- 現在のリポジトリにはサイト実装がないため、フレームワーク固有の `src/data` は作らない。実装導入時に既存構造へ適合する配置を検討する。
- 正式情報の変更指示を受けた場合は、次の順で必要な箇所だけを同期する。
  1. 構造化データ
  2. `SITE_FACTS.md`
  3. 必要なサイト表示
  4. `CHANGELOG.md`
- HTML 等へ共通の重要情報を重複して直接記述しない。ただし、既存表示を壊す移行は行わない。

## Amazon 商品追加フロー

1. 商品情報の提供元を確認する。
2. ユーザー提供の Amazon アソシエイト URL とタグを確認する。
3. 既存の商品データ構造を確認する。
4. 指定された商品データだけを追加する。
5. Amazon リンクとアソシエイトタグを確認する。
6. 独自紹介文は確認できる事実だけで作成し、根拠のない性能、価格、在庫、評価、レビューを追加しない。
7. 既存商品の表示・リンク・画像を変更しない。
8. ビルドを実行する。
9. `node scripts/check-amazon-links.mjs` を実行する。
10. スマートフォン表示を確認する。
11. 公開前チェックを実行する。
12. `CHANGELOG.md` に記録する。

Amazon 商品を追加・変更した場合は、作業完了前に必ずリンクチェックスクリプトを実行する。結果は確認専用であり、スクリプトや AI が URL を自動修正してはならない。

Amazon 関連の詳細は `AFFILIATE_RULES.md` に従う。

## 変更と検証

- 変更前に `SITE_FACTS.md`、`AFFILIATE_RULES.md`、対象実装、既存ワークフローを確認する。
- サイト本体を変更した場合は原則として `CHANGELOG.md` に追記する。
- 公開HTMLを変更した場合は `node scripts/audit-seo.mjs` を実行し、title、description、canonical、H1、構造化データ、内部リンク、画像altを確認する。
- 公開前に `CHECKLIST.md` の該当項目を確認する。
- ビルド失敗、重大な実行時エラー、重大な表示崩れがある状態を公開しない。
- 自動確認だけでスマートフォン・PC の表示を確認済みと断定しない。実機または同等のビューポート確認を記録する。

## GitHub の安全ルール

- `main` ブランチは本番公開用として扱う。
- 大きな変更は可能なら別ブランチで行う。
- API キー、パスワード、Amazon 関連の秘密情報をコードや GitHub に保存しない。
- 秘密情報は GitHub Secrets 等を使用する。
- `.env` を使う場合は `.gitignore` 対象であることを確認する。
- GitHub Actions には必要最小限の権限だけを与える。
- 不要な外部 Action や依存パッケージを追加しない。
- 既存の正常な公開設定を、検証なしに全面書き換えない。

## GitHub Pages スナップショット

- GitHub Pages用の公開ファイルは `docs/` に置く。
- `docs/` を更新するときは `node scripts/snapshot-public-site.mjs` を明示的に実行し、定期実行で公開サイトを自動スクレイピングしない。
- スナップショット処理はAmazon URL、商品文章、画像URLを変更しない。
- 生成後は `docs/sitemap.xml`、`docs/robots.txt`、内部リンク、モバイル表示、Amazonリンク件数を確認する。
